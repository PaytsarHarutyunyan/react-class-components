import React from 'react';
import { ControlledForm } from '../components/HookForm/HookForm';

const ControlledFormPage: React.FC = () => {
    return (
        <div>
            <h2>Controlled Form</h2>
            <ControlledForm />
        </div>
    );
};

export default ControlledFormPage;
